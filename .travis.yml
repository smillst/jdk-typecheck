language: java
jdk:
  - oraclejdk8

sudo: false

env:
  - GROUP=formatter
  - GROUP=index
  - GROUP=interning
  - GROUP=lock
  - GROUP=nullness-fbc
  - GROUP=nullness-raw
  - GROUP=regex
  - GROUP=signature
  - GROUP=value

script:  ./.travis-build.sh $GROUP

git:
  depth: 3
